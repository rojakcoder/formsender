This repository sends messages to specified Telegram groups.

It is able to receive requests from sites that are specified in the
configuration files as allowable origins.

## Running

The service can be run like so:

```bash
npm start
```

